int main() {
    int x,i;
    printf("Moi ban nhap so n=");
    scanf("%d", &x);
    printf("Cac uoc so cua %d la:", x);
    for(i=1; i<=x; i++){
    if(x%i==0)printf("%d", i);
    }

    return 0;
}
