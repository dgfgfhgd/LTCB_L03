#include <stdio.h>

int main() {
    int i,n;
    printf("Moi ban nhap so n=");
    scanf("%d",&n);
    int s=0;
    for(i=1;i<n;i++){
        if(n%i==0)
        s+=i;
}
    if(s==n){
        printf("%d la so hoan hao", n);}
    else{
        printf("%d la so khong hoan hao", n);}
    return 0;
}
